title: kicad-autorouter
description: Pure Python autorouter for KiCad — no Java required
remote_theme: pages-themes/minimal@v0.2.0
plugins:
  - jekyll-remote-theme

baseurl: /kicad-autorouter
url: https://danwillis-aethl.github.io

exclude:
  - Gemfile
  - Gemfile.lock

defaults:
  - scope:
      path: ""
    values:
      layout: default
